//! Auth configuration for SalesInvoice handlers
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Implements `ResourcePolicy<SalesInvoice>` with entity-specific permission
//! string constants. Guard middleware logic lives in backbone-auth.

use backbone_auth::{ResourcePolicy, ResourceAction, PermissionGuard, ServicePermissionGuard};
use async_trait::async_trait;

use crate::application::service::SalesInvoiceService;
use crate::domain::entity::SalesInvoice;

/// Permission string constants for SalesInvoice operations.
pub mod permissions {
    pub const LIST:        &str = "sales_invoice:list";
    pub const READ:        &str = "sales_invoice:read";
    pub const CREATE:      &str = "sales_invoice:create";
    pub const UPDATE:      &str = "sales_invoice:update";
    pub const DELETE:      &str = "sales_invoice:delete";
    pub const RESTORE:     &str = "sales_invoice:restore";
    pub const TRASH:       &str = "sales_invoice:trash";
    pub const EMPTY_TRASH: &str = "sales_invoice:empty_trash";
    pub const BULK_CREATE: &str = "sales_invoice:bulk_create";
    pub const UPSERT:      &str = "sales_invoice:upsert";
}

/// Resource policy for SalesInvoice — maps CRUD actions to permission strings.
pub struct SalesInvoicePolicy;

#[async_trait]
impl ResourcePolicy<SalesInvoice> for SalesInvoicePolicy {
    async fn can(
        &self,
        action: ResourceAction,
        _entity: &SalesInvoice,
        ctx: &backbone_auth::middleware::AuthContext,
    ) -> bool {
        let required = match action {
            ResourceAction::Read | ResourceAction::List => permissions::READ,
            ResourceAction::Create => permissions::CREATE,
            ResourceAction::Update | ResourceAction::Patch => permissions::UPDATE,
            ResourceAction::Delete | ResourceAction::HardDelete => permissions::DELETE,
            ResourceAction::Restore => permissions::RESTORE,
            ResourceAction::Custom(_) => return false,
        };
        ctx.permissions.iter().any(|p| p == required)
    }
}

/// Permission guard for SalesInvoice handlers.
///
/// Wraps `SalesInvoiceService` and checks `SalesInvoicePolicy` before delegating.
pub type SalesInvoiceGuard = PermissionGuard<SalesInvoice>;

/// Service-integrated permission guard for SalesInvoice (for use in generated handlers).
pub type SalesInvoiceServiceGuard = ServicePermissionGuard<SalesInvoice, SalesInvoiceService, SalesInvoicePolicy>;

// <<< CUSTOM
// END CUSTOM
