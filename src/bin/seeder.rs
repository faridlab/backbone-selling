//! Database seeder binary for module: selling
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Run with: cargo run --bin selling-seeder
//! Or via CLI: backbone seed run --module selling
//!
//! This binary runs all registered seeders in order.

use anyhow::Result;
use sqlx::postgres::PgPoolOptions;
use std::env;

// Import seeders
use backbone_selling::seeders::SeedQuotationSeeder;
use backbone_selling::seeders::SeedQuotationItemSeeder;
use backbone_selling::seeders::SeedSalesInvoiceSeeder;
use backbone_selling::seeders::SeedSalesInvoiceItemSeeder;
use backbone_selling::seeders::SeedSalesOrderSeeder;
use backbone_selling::seeders::SeedSalesOrderItemSeeder;
use backbone_selling::seeders::SeedSalesTeamSeeder;
use backbone_selling::seeders::SeedSalesPersonAllocationSeeder;
use backbone_selling::seeders::Seeder;

#[tokio::main]
async fn main() -> Result<()> {
    let args: Vec<String> = env::args().collect();
    let force = args.iter().any(|a| a == "--force");
    let filter: Option<&str> = args.iter()
        .skip(1)
        .find(|a| !a.starts_with("-"))
        .map(|s| s.as_str());

    let database_url = env::var("DATABASE_URL")
        .expect("DATABASE_URL must be set");

    println!("Connecting to database...");

    let pool = PgPoolOptions::new()
        .max_connections(5)
        .connect(&database_url)
        .await?;

    println!("Connected to database");
    println!();
    println!("Running seeders for module: selling");
    println!();

    // Register seeders in order
    let mut seeders: Vec<Box<dyn Seeder + Send + Sync>> = Vec::new();
    seeders.push(Box::new(SeedQuotationSeeder::new()));
    seeders.push(Box::new(SeedQuotationItemSeeder::new()));
    seeders.push(Box::new(SeedSalesInvoiceSeeder::new()));
    seeders.push(Box::new(SeedSalesInvoiceItemSeeder::new()));
    seeders.push(Box::new(SeedSalesOrderSeeder::new()));
    seeders.push(Box::new(SeedSalesOrderItemSeeder::new()));
    seeders.push(Box::new(SeedSalesTeamSeeder::new()));
    seeders.push(Box::new(SeedSalesPersonAllocationSeeder::new()));

    // Sort by order
    seeders.sort_by_key(|s| s.order());

    // Run seeders
    for seeder in &seeders {
        let name = seeder.name();

        // Apply filter if specified
        if let Some(f) = filter {
            if !name.contains(f) {
                continue;
            }
        }

        if force {
            println!("   Running {}...", name);
            seeder.run(&pool).await?;
            println!("   {} completed", name);
        } else {
            println!("   Checking {}...", name);
            if seeder.should_run(&pool).await? {
                println!("   Running {}...", name);
                seeder.run(&pool).await?;
                println!("   {} completed", name);
            } else {
                println!("   {} already applied, skipping", name);
            }
        }
    }

    println!();
    println!("All seeders completed successfully!");

    Ok(())
}
