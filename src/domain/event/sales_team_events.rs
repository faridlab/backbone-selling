//! Domain events for SalesTeam entity
//!
//! Generated by metaphor-schema. Do not edit manually.

use backbone_messaging::CrudEvent;
use crate::domain::entity::SalesTeam;

/// Domain events for SalesTeam — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<SalesTeam>)` variant.
pub type SalesTeamEvent = CrudEvent<SalesTeam>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for SalesTeam events.
pub type SalesTeamEventPublisher = Arc<dyn CrudEventPublisher<SalesTeam> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopSalesTeamEventPublisher = NoOpCrudEventPublisher<SalesTeam>;
