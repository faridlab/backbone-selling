//! Repository trait for DeliveryCarrier aggregate
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the DeliveryCarrier aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::DeliveryCarrier;

/// Pagination parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct DeliveryCarrierPaginationParams {
    pub page: u32,
    pub per_page: u32,
}

impl DeliveryCarrierPaginationParams {
    pub fn new(page: u32, per_page: u32) -> Self {
        Self { page, per_page }
    }

    pub fn offset(&self) -> u64 {
        ((self.page.saturating_sub(1)) * self.per_page) as u64
    }

    pub fn limit(&self) -> u64 {
        self.per_page as u64
    }
}

/// Paginated result for list queries
#[derive(Debug, Clone)]
pub struct DeliveryCarrierPaginatedResult {
    pub data: Vec<DeliveryCarrier>,
    pub total: u64,
    pub page: u32,
    pub per_page: u32,
}

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct DeliveryCarrierFilter {
    pub company_id: Option<Uuid>,
    pub name: Option<String>,
    pub active: Option<bool>,
    pub tracking_url_template: Option<String>,
}

impl DeliveryCarrierFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.company_id.is_some() || self.name.is_some() || self.active.is_some() || self.tracking_url_template.is_some()
    }
}

/// Repository trait for DeliveryCarrier aggregate
///
/// This trait defines the persistence contract for DeliveryCarrier entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait DeliveryCarrierRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new delivery_carrier entity
    async fn save(&self, entity: &DeliveryCarrier) -> Result<DeliveryCarrier>;

    /// Find delivery_carrier by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<DeliveryCarrier>>;

    /// Find all delivery_carrier entities
    async fn find_all(&self) -> Result<Vec<DeliveryCarrier>>;

    /// Update delivery_carrier by ID
    async fn update(&self, id: &str, entity: &DeliveryCarrier) -> Result<Option<DeliveryCarrier>>;

    /// Delete delivery_carrier by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List delivery_carrier with pagination
    async fn list(&self, params: DeliveryCarrierPaginationParams) -> Result<DeliveryCarrierPaginatedResult>;

    /// List delivery_carrier with pagination and filters
    async fn list_with_filters(&self, params: DeliveryCarrierPaginationParams, filters: DeliveryCarrierFilter) -> Result<DeliveryCarrierPaginatedResult>;

    /// Count all delivery_carrier entities
    async fn count(&self) -> Result<u64>;

    /// Count delivery_carrier entities matching filters
    async fn count_with_filters(&self, filters: DeliveryCarrierFilter) -> Result<u64>;

    /// Check if delivery_carrier exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete delivery_carrier by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted delivery_carrier by ID
    async fn restore(&self, id: &str) -> Result<Option<DeliveryCarrier>>;

    /// List soft-deleted delivery_carrier entities
    async fn list_deleted(&self, params: DeliveryCarrierPaginationParams) -> Result<DeliveryCarrierPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save delivery_carrier entities
    async fn bulk_save(&self, entities: &[DeliveryCarrier]) -> Result<Vec<DeliveryCarrier>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
