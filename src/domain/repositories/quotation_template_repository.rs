//! Repository trait for QuotationTemplate aggregate
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the QuotationTemplate aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::QuotationTemplate;

/// Pagination parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct QuotationTemplatePaginationParams {
    pub page: u32,
    pub per_page: u32,
}

impl QuotationTemplatePaginationParams {
    pub fn new(page: u32, per_page: u32) -> Self {
        Self { page, per_page }
    }

    pub fn offset(&self) -> u64 {
        ((self.page.saturating_sub(1)) * self.per_page) as u64
    }

    pub fn limit(&self) -> u64 {
        self.per_page as u64
    }
}

/// Paginated result for list queries
#[derive(Debug, Clone)]
pub struct QuotationTemplatePaginatedResult {
    pub data: Vec<QuotationTemplate>,
    pub total: u64,
    pub page: u32,
    pub per_page: u32,
}

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct QuotationTemplateFilter {
    pub company_id: Option<Uuid>,
    pub name: Option<String>,
    pub default_notes: Option<String>,
}

impl QuotationTemplateFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.company_id.is_some() || self.name.is_some() || self.default_notes.is_some()
    }
}

/// Repository trait for QuotationTemplate aggregate
///
/// This trait defines the persistence contract for QuotationTemplate entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait QuotationTemplateRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new quotation_template entity
    async fn save(&self, entity: &QuotationTemplate) -> Result<QuotationTemplate>;

    /// Find quotation_template by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<QuotationTemplate>>;

    /// Find all quotation_template entities
    async fn find_all(&self) -> Result<Vec<QuotationTemplate>>;

    /// Update quotation_template by ID
    async fn update(&self, id: &str, entity: &QuotationTemplate) -> Result<Option<QuotationTemplate>>;

    /// Delete quotation_template by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List quotation_template with pagination
    async fn list(&self, params: QuotationTemplatePaginationParams) -> Result<QuotationTemplatePaginatedResult>;

    /// List quotation_template with pagination and filters
    async fn list_with_filters(&self, params: QuotationTemplatePaginationParams, filters: QuotationTemplateFilter) -> Result<QuotationTemplatePaginatedResult>;

    /// Count all quotation_template entities
    async fn count(&self) -> Result<u64>;

    /// Count quotation_template entities matching filters
    async fn count_with_filters(&self, filters: QuotationTemplateFilter) -> Result<u64>;

    /// Check if quotation_template exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete quotation_template by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted quotation_template by ID
    async fn restore(&self, id: &str) -> Result<Option<QuotationTemplate>>;

    /// List soft-deleted quotation_template entities
    async fn list_deleted(&self, params: QuotationTemplatePaginationParams) -> Result<QuotationTemplatePaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save quotation_template entities
    async fn bulk_save(&self, entities: &[QuotationTemplate]) -> Result<Vec<QuotationTemplate>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
