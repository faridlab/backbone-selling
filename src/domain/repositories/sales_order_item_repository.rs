//! Repository trait for SalesOrderItem aggregate
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the SalesOrderItem aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::SalesOrderItem;

/// Pagination parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct SalesOrderItemPaginationParams {
    pub page: u32,
    pub per_page: u32,
}

impl SalesOrderItemPaginationParams {
    pub fn new(page: u32, per_page: u32) -> Self {
        Self { page, per_page }
    }

    pub fn offset(&self) -> u64 {
        ((self.page.saturating_sub(1)) * self.per_page) as u64
    }

    pub fn limit(&self) -> u64 {
        self.per_page as u64
    }
}

/// Paginated result for list queries
#[derive(Debug, Clone)]
pub struct SalesOrderItemPaginatedResult {
    pub data: Vec<SalesOrderItem>,
    pub total: u64,
    pub page: u32,
    pub per_page: u32,
}

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct SalesOrderItemFilter {
    pub order_id: Option<Uuid>,
    pub company_id: Option<Uuid>,
    pub item_id: Option<Uuid>,
    pub description: Option<String>,
}

impl SalesOrderItemFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.order_id.is_some() || self.company_id.is_some() || self.item_id.is_some() || self.description.is_some()
    }
}

/// Repository trait for SalesOrderItem aggregate
///
/// This trait defines the persistence contract for SalesOrderItem entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait SalesOrderItemRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new sales_order_item entity
    async fn save(&self, entity: &SalesOrderItem) -> Result<SalesOrderItem>;

    /// Find sales_order_item by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<SalesOrderItem>>;

    /// Find all sales_order_item entities
    async fn find_all(&self) -> Result<Vec<SalesOrderItem>>;

    /// Update sales_order_item by ID
    async fn update(&self, id: &str, entity: &SalesOrderItem) -> Result<Option<SalesOrderItem>>;

    /// Delete sales_order_item by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List sales_order_item with pagination
    async fn list(&self, params: SalesOrderItemPaginationParams) -> Result<SalesOrderItemPaginatedResult>;

    /// List sales_order_item with pagination and filters
    async fn list_with_filters(&self, params: SalesOrderItemPaginationParams, filters: SalesOrderItemFilter) -> Result<SalesOrderItemPaginatedResult>;

    /// Count all sales_order_item entities
    async fn count(&self) -> Result<u64>;

    /// Count sales_order_item entities matching filters
    async fn count_with_filters(&self, filters: SalesOrderItemFilter) -> Result<u64>;

    /// Check if sales_order_item exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete sales_order_item by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted sales_order_item by ID
    async fn restore(&self, id: &str) -> Result<Option<SalesOrderItem>>;

    /// List soft-deleted sales_order_item entities
    async fn list_deleted(&self, params: SalesOrderItemPaginationParams) -> Result<SalesOrderItemPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save sales_order_item entities
    async fn bulk_save(&self, entities: &[SalesOrderItem]) -> Result<Vec<SalesOrderItem>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
