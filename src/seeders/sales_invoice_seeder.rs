//! SalesInvoice seeder
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Seeds initial SalesInvoice data into the database.

use anyhow::Result;
use async_trait::async_trait;
use sqlx::PgPool;

use super::Seeder;

// ============================================================================
// SALESINVOICE SEEDER
// ============================================================================

/// Seeder for sales_invoice
pub struct SeedSalesInvoiceSeeder;

impl SeedSalesInvoiceSeeder {
    pub fn new() -> Self {
        Self
    }
}

impl Default for SeedSalesInvoiceSeeder {
    fn default() -> Self {
        Self::new()
    }
}

#[async_trait]
impl Seeder for SeedSalesInvoiceSeeder {
    fn name(&self) -> &'static str {
        "SeedSalesInvoiceSeeder"
    }

    fn order(&self) -> i32 {
        3
    }

    async fn should_run(&self, pool: &PgPool) -> Result<bool> {
        // Check if selling.sales_invoices table has any data
        let count: (i64,) = sqlx::query_as("SELECT COUNT(*) FROM selling.sales_invoices")
            .fetch_one(pool)
            .await?;
        Ok(count.0 == 0)
    }

    async fn run(&self, pool: &PgPool) -> Result<()> {
        // Run the SQL seed file
        let sql = include_str!("../../migrations/seeds/sales_invoice_seed.sql");
        sqlx::raw_sql(sql).execute(pool).await?;
        Ok(())
    }

    async fn rollback(&self, pool: &PgPool) -> Result<()> {
        sqlx::query("DELETE FROM selling.sales_invoices")
            .execute(pool)
            .await?;
        Ok(())
    }
}

// ============================================================================
// CUSTOM SEED DATA
// ============================================================================

// <<< CUSTOM SEED DATA START >>>
// Add custom seed data methods here
// <<< CUSTOM SEED DATA END >>>

