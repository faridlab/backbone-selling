//! SalesInvoice API Integration Tests
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Tests the SalesInvoice CRUD API endpoints.

use chrono::Utc;
use serde_json::{json, Value};
use uuid::Uuid;

use super::crud_test_base::{CrudTestConfig, GenericCrudTest, TestDataGenerator};
use crate::integration::helpers::CommonUtils;

// ============================================================================
// SALESINVOICE TEST DATA GENERATOR
// ============================================================================

/// Test data generator for SalesInvoice
pub struct SalesInvoiceTestData;

impl TestDataGenerator for SalesInvoiceTestData {
    fn generate_create_payload(&self, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": Uuid::new_v4().to_string(),
            "invoice_number": format!("Test {}", Uuid::new_v4().to_string().split('-').next().unwrap()),
            "sales_order_id": null,
            "company_id": Uuid::new_v4().to_string(),
            "branch_id": null,
            "customer_id": Uuid::new_v4().to_string(),
            "status": "draft",
            "invoice_date": Utc::now().format("%Y-%m-%d").to_string(),
            "due_date": null,
            "currency": format!("Test {}", Uuid::new_v4().to_string().split('-').next().unwrap()),
            "subtotal": 0,
            "tax_rate": 0,
            "tax_amount": 0,
            "total": 0,
            "outstanding_amount": 0,
            "receivable_account_id": Uuid::new_v4().to_string(),
            "tax_output_account_id": null,
            "posting_state": "pending",
            "journal_id": null,
            "accounting_post_id": null,
            "posted_at": now,
            "notes": null,
            "metadata": json!({}),
        })
    }

    fn generate_update_payload(&self, id: &str, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": id,
            "invoice_number": format!("Test {}", Uuid::new_v4().to_string().split('-').next().unwrap()),
            "sales_order_id": null,
            "company_id": Uuid::new_v4().to_string(),
            "branch_id": null,
            "customer_id": Uuid::new_v4().to_string(),
            "status": "draft",
            "invoice_date": Utc::now().format("%Y-%m-%d").to_string(),
            "due_date": null,
            "currency": format!("Test {}", Uuid::new_v4().to_string().split('-').next().unwrap()),
            "subtotal": 0,
            "tax_rate": 0,
            "tax_amount": 0,
            "total": 0,
            "outstanding_amount": 0,
            "receivable_account_id": Uuid::new_v4().to_string(),
            "tax_output_account_id": null,
            "posting_state": "pending",
            "journal_id": null,
            "accounting_post_id": null,
            "posted_at": now,
            "notes": null,
            "metadata": json!({}),
        })
    }

    fn generate_invalid_payload(&self) -> Value {
        json!({
            // Missing required fields
        })
    }
}

// ============================================================================
// SALESINVOICE API TEST
// ============================================================================

/// SalesInvoice API test suite
pub struct SalesInvoiceApiTest {
    inner: GenericCrudTest<SalesInvoiceTestData>,
}

impl SalesInvoiceApiTest {
    pub fn new() -> Self {
        let mut config = CrudTestConfig::new("/api/v1/sales_invoices", "SalesInvoice");
        config.supports_soft_delete = true;
        Self {
            inner: GenericCrudTest::new(config, SalesInvoiceTestData),
        }
    }

    pub async fn run_all(&mut self) -> Vec<crate::integration::framework::TestResult> {
        self.inner.run_all().await
    }
}

impl Default for SalesInvoiceApiTest {
    fn default() -> Self {
        Self::new()
    }
}

// ============================================================================
// TESTS
// ============================================================================

#[cfg(test)]
mod tests {
    use super::*;

    #[tokio::test]
    async fn test_sales_invoice_crud() {
        let mut test = SalesInvoiceApiTest::new();
        let results = test.run_all().await;

        for result in &results {
            println!("{:?}", result);
        }

        // Check all tests passed
        let failed: Vec<_> = results.iter().filter(|r| !r.success).collect();
        assert!(failed.is_empty(), "Some tests failed: {:?}", failed);
    }
}

// ============================================================================
// CUSTOM TESTS
// ============================================================================

// <<< CUSTOM TESTS START >>>
// Add SalesInvoice specific tests here
// <<< CUSTOM TESTS END >>>
