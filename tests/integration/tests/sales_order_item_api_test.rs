//! SalesOrderItem API Integration Tests
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Tests the SalesOrderItem CRUD API endpoints.

use serde_json::{json, Value};
use uuid::Uuid;

use super::crud_test_base::{CrudTestConfig, GenericCrudTest, TestDataGenerator};
use crate::integration::framework::ApiTest;
use crate::integration::helpers::CommonUtils;

// ============================================================================
// SALESORDERITEM TEST DATA GENERATOR
// ============================================================================

/// Test data generator for SalesOrderItem
pub struct SalesOrderItemTestData;

impl TestDataGenerator for SalesOrderItemTestData {
    fn generate_create_payload(&self, _utils: &CommonUtils) -> Value {
        json!({
            "id": Uuid::new_v4().to_string(),
            "order_id": Uuid::new_v4().to_string(),
            "item_id": Uuid::new_v4().to_string(),
            "description": null,
            "quantity": 0,
            "unit_price": 0,
            "line_discount": 0,
            "line_amount": 0,
            "billed_qty": 0,
            "metadata": json!({}),
        })
    }

    fn generate_update_payload(&self, id: &str, _utils: &CommonUtils) -> Value {
        json!({
            "id": id,
            "order_id": Uuid::new_v4().to_string(),
            "item_id": Uuid::new_v4().to_string(),
            "description": null,
            "quantity": 0,
            "unit_price": 0,
            "line_discount": 0,
            "line_amount": 0,
            "billed_qty": 0,
            "metadata": json!({}),
        })
    }

    fn generate_invalid_payload(&self) -> Value {
        json!({
            // Missing required fields
        })
    }

    async fn seed_dependencies(&self, api: &ApiTest) -> Vec<(String, String)> {
        let mut deps: Vec<(String, String)> = Vec::new();
        if let Some(id) = super::crud_test_base::create_and_get_id(api, "/api/v1/sales_orders", &super::sales_order_api_test::SalesOrderTestData).await {
            deps.push(("order_id".to_string(), id));
        }
        deps
    }
}

// ============================================================================
// SALESORDERITEM API TEST
// ============================================================================

/// SalesOrderItem API test suite
pub struct SalesOrderItemApiTest {
    inner: GenericCrudTest<SalesOrderItemTestData>,
}

impl SalesOrderItemApiTest {
    pub fn new() -> Self {
        let mut config = CrudTestConfig::new("/api/v1/sales_order_items", "SalesOrderItem");
        config.supports_soft_delete = true;
        Self {
            inner: GenericCrudTest::new(config, SalesOrderItemTestData),
        }
    }

    pub async fn run_all(&mut self) -> Vec<crate::integration::framework::TestResult> {
        self.inner.run_all().await
    }
}

impl Default for SalesOrderItemApiTest {
    fn default() -> Self {
        Self::new()
    }
}

// ============================================================================
// TESTS
// ============================================================================

#[cfg(test)]
mod tests {
    use super::*;

    #[tokio::test]
    async fn test_sales_order_item_crud() {
        let mut test = SalesOrderItemApiTest::new();
        let results = test.run_all().await;

        for result in &results {
            println!("{:?}", result);
        }

        // Check all tests passed
        let failed: Vec<_> = results.iter().filter(|r| !r.success).collect();
        assert!(failed.is_empty(), "Some tests failed: {:?}", failed);
    }
}

// ============================================================================
// CUSTOM TESTS
// ============================================================================

// <<< CUSTOM TESTS START >>>
// Add SalesOrderItem specific tests here
// <<< CUSTOM TESTS END >>>
